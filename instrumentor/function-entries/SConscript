Import('env')
env = env.Copy()
env.AppendUnique(CCFLAGS=['--sampler-scheme=function-entries'])

Alias('test:function-entries', env.Expect([
    env.CBIResolved('test.c'),
    ]))

Alias('test', 'test:function-entries')
