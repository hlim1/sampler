#!/bin/sh

top_srcdir='@top_srcdir@'
XMLLINT="@XMLLINT@"

case $1 in
  (*.desktop) exec desktop-file-validate $1 ;;
  (*.py) exec pychecker --quiet $1 ;;
  (*.server)  exec $XMLLINT --valid --noout $1 ;;
  (*) false ;;
esac
