#!/usr/bin/perl -w

use strict;
use Miner;
use FileHandle;


local $\ = "\n";

my $summary = new FileHandle 'results.dat', 'w';
$summary->print('result');

foreach my $seed ($ARGV[0] .. $ARGV[1]) {
    $summary->print(Miner::loadResult $seed);
}
