AT_INIT(bounds)

AT_SETUP(enum)
AT_CHECK(
set -e
cp $abs_srcdir/enum.c .
cp $abs_srcdir/enum.expected expout
$abs_top_builddir/instrumentor/driver/sampler-cc-here --sampler-scheme=bounds --assign-across-pointer -o enum enum.c -save-temps
SAMPLER_FILE=trace SAMPLER_SPARSITY=1 ./enum
$abs_top_srcdir/tools/extract-report samples <trace | $abs_top_srcdir/tools/resolve-samples enum | cut -f2- | sort -t '	'
, 0, expout)
AT_CLEANUP
