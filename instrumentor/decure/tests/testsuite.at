AT_INIT(decure)

AT_SETUP(test)
AT_CHECK(
set -e
cp $abs_srcdir/test.c .
$abs_top_builddir/instrumentor/driver/main decure -o test $abs_srcdir/test.c
SAMPLER_SPARSITY=1 ./test, 0, [Hi!
], stderr)
AT_CLEANUP
