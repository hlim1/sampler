#!/usr/bin/perl -w

use strict;
use FileHandle;


print "benchmark\tfunction\tloopless\n";


my @logs = glob '*/loopless';

foreach my $log (@logs) {
    $log =~ /^(.+)\/loopless/ or die;
    my $benchmark = $1;

    my $file = new FileHandle($log, 'r') or die;
    print "$1\t$_" while <$file>;
}
